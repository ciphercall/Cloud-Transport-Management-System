@using Cloud_Transport.Models
@using Microsoft.Ajax.Utilities
@model Cloud_Transport.Models.PageModel

@{
    ViewBag.Title = "Get_ListofCostPool";

    Layout = null;

    if (HttpContext.Current.Session["LoggedUserType"] == null)
    {
        Response.Redirect(Url.Action("Index", "Login"));
    }

    var LoggedUserTp = HttpContext.Current.Session["LoggedUserType"].ToString();
    var LoggedUserStatus = HttpContext.Current.Session["LoggedUserStatus"].ToString();
    var LoggedCompanyStatus = HttpContext.Current.Session["LoggedCompanyStatus"].ToString();

    if ((LoggedUserTp == "CompanyAdmin" || LoggedUserTp == "UserAdmin" || LoggedUserTp == "User") && LoggedUserStatus == "A" && LoggedCompanyStatus == "A")
    {

    }
    else
    {
        Response.Redirect(Url.Action("Index", "Logout"));
    }

    CnfDbContext db = new CnfDbContext();
    var LoggedCompId = HttpContext.Current.Session["loggedCompID"].ToString();
    Int64 compid = Convert.ToInt64(LoggedCompId);
    Int64 loggedUserID = Convert.ToInt64(HttpContext.Current.Session["loggedUserID"].ToString());


    var permission_Check = from m in db.AslRoleDbSet
                           where m.COMPID == compid && m.USERID == loggedUserID
                               && m.CONTROLLERNAME == "GlListReport" && m.ACTIONNAME == "Get_ListofCostPool"
                           select m;
    var Status = "";
    foreach (var check in permission_Check)
    {
        Status = check.STATUS;
    }

    if (Status == "A")
    {
    }
    else
    {
        Response.Redirect(Url.Action("Index", "Logout"));
    }





    string companyName = "", address = "", contactNo = "";
    var SelectCompanyName = from n in db.AslCompanyDbSet

                            where n.COMPID == compid
                            select new { n.COMPNM, n.ADDRESS, n.CONTACTNO };

    foreach (var n in SelectCompanyName)
    {
        companyName = n.COMPNM;
        address = n.ADDRESS;
        contactNo = n.CONTACTNO;
    }
    var compNm = companyName;
    var Address = address;
    var contactno = contactNo;

    TimeZoneInfo timeZoneInfo;
    timeZoneInfo = TimeZoneInfo.FindSystemTimeZoneById("Central Asia Standard Time");
    DateTime currentDateTime = TimeZoneInfo.ConvertTime(DateTime.Now, timeZoneInfo);
}

<!DOCTYPE html>

<html lang="en">
<head>
    <link href="~/Content/reportcss/style.css" rel="stylesheet" />
    @*<script type="text/javascript">
            function ClosePrint() {
                var print = document.getElementById("print");
                print.style.visibility = "hidden";
                //            print.display = false;

                window.print();
            }
        </script>*@

</head>
<body style=" background-color:#fafafa;">
    <div style="width: 960px; margin: 0px auto; border: 1px solid #d3d3d3; border-radius: 5px; box-shadow: 0 0 5px rgba(0, 0, 0, 0.1); background: none repeat scroll 0 0 white;">
        <table id="mainTable" border="0" width="100%">
            <thead>
                <tr>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>
                    <td width="5%"></td>

                </tr>
                <tr style="background-color: #00689b">
                    <td colspan="14">
                        <h3 style="color: white;padding:4px">@compNm</h3>
                        @*<p style="color:white;padding-left:4px">@Address<span>  @contactno</span></p>*@
                    </td>

                    <td colspan="6" style="color: white;padding:8px" align="right">
                        Powered By<br />Alchemy Software
                    </td>

                </tr>

                <tr>
                    <td colspan="14" align="left" style="padding: 4px; font-weight: bold">List of Vehicle</td>
                    <td align="right" colspan="6" style="padding:8px">Print:@currentDateTime.ToString("dd-MM-yy hh:mm tt")</td>
                </tr>

                <tr style="background-color: #00689b; outline: thin solid">
                    <td style="color: white;padding:4px" colspan="6" align="left">Vehicle Name</td>
                    <td style="color: white; padding: 4px" colspan="3" align="center">Vehicle ShortID</td>
                    <td style="color: white; padding: 4px" colspan="3" align="center">Purchase Date</td>
                    <td style="color: white;padding:4px" colspan="3" align="center">Registration Date</td>
                    <td style="color: white;padding:4px" colspan="5" align="center">Details</td>
                </tr>

            </thead>
            <tbody>
                @{
                    Int64 maxHeadCD = Convert.ToInt64((from m in db.GLCostPMSTDbSet
                                                       where m.COMPID == compid
                                                       select m.COSTCID).Max());
                    Int64 minHeadCD = Convert.ToInt64((from m in db.GLCostPMSTDbSet
                                                       where m.COMPID == compid
                                                       select m.COSTCID).Min());

                }
                @for (Int64 i = minHeadCD; i <= maxHeadCD; i++)
                {
                    Int64 Current_HeadCD = i;

                    var getList = (from m in db.GlCostPoolDbSet
                                   where m.COMPID == compid && m.COSTCID == Current_HeadCD
                                   select m).ToList();

                    if (getList.Count != 0)
                    {
                        string HeadName = "";
                        var getHeadName = (from m in db.GLCostPMSTDbSet where m.COMPID == compid && m.COSTCID == Current_HeadCD select new { m.COSTCNM });
                        foreach (var x in getHeadName)
                        {
                            HeadName = x.COSTCNM;
                        }

                        <tr>
                            <td align="left" colspan="18">
                                <strong>@HeadName</strong>
                            </td>

                        </tr>

                        Int64 count = Convert.ToInt64(getList.Count);
                        Int64 j = 0;

                        foreach (var item in getList)
                        {
                            j++;
                            if (j == count)
                            {
                                <tr style="outline:thin solid">
                                    <td colspan="6" align="left" style="padding-left:60px">
                                        @Html.DisplayFor(modelItem => item.COSTPNM)
                                    </td>
                                    <td colspan="3" align="left" style="padding-left: 12px">
                                        @Html.DisplayFor(modelItem => item.COSTPSID)
                                    </td>
                                    <td colspan="3" align="center" style="padding-left: 12px">
                                        @Html.DisplayFor(modelItem => item.BUYDT)
                                    </td>
                                    <td colspan="3" align="center" style="padding-left:12px">
                                        @Html.DisplayFor(modelItem => item.REGDT)
                                    </td>
                                    <td colspan="5" align="left">
                                        @Html.DisplayFor(modelItem => item.REMARKS)
                                    </td>
                                </tr>
                            }
                            else
                            {
                                <tr style="outline:thin solid">
                                    <td colspan="6" align="left" style="padding-left:60px">
                                        @Html.DisplayFor(modelItem => item.COSTPNM)
                                    </td>
                                    <td colspan="3" align="left" style="padding-left: 12px">
                                        @Html.DisplayFor(modelItem => item.COSTPSID)
                                    </td>
                                    <td colspan="3" align="center" style="padding-left: 12px">
                                        @Html.DisplayFor(modelItem => item.BUYDT)
                                    </td>
                                    <td colspan="3" align="center" style="padding-left:12px">
                                        @Html.DisplayFor(modelItem => item.REGDT)
                                    </td>
                                    <td colspan="5" align="left">
                                        @Html.DisplayFor(modelItem => item.REMARKS)
                                    </td>
                                </tr>
                            }

                        }

                        <tr>
                            <td><br /></td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>


</body>
</html>
